# Django Task Manager

Система управління проєктами та завданнями для Django проєктів.

## 📋 Функціональність

- ✅ Управління проєктами з унікальними ключами
- ✅ Гнучка система завдань з автоматичною нумерацією
- ✅ Підтримка глобальних та проєктних статусів
- ✅ Організація по підрозділам (ІАРМ, ЗБСІ, СД КТК, Майстерня тощо)
- ✅ Система пріоритетів завдань
- ✅ Відстеження термінів виконання
- ✅ Автоматична історія змін
- ✅ Коментарі до завдань
- ✅ Статистика та звітність

## 🚀 Швидкий старт

### 1. Установка

```bash
# Скопіюйте файли в вашу Django app
yourapp/
├── models.py          # Моделі
├── admin.py           # Налаштування адмінки
├── signals.py         # Сигнали та автоматизація
└── management/
    └── commands/
        └── init_task_manager.py  # Команда ініціалізації
```

### 2. Налаштування settings.py

```python
INSTALLED_APPS = [
    # ...
    'yourapp',  # Ваша app з Task Manager
]
```

### 3. Підключення сигналів

У файлі `yourapp/apps.py`:

```python
from django.apps import AppConfig

class YourAppConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'yourapp'
    
    def ready(self):
        import yourapp.signals  # Імпортуємо сигнали
```

У файлі `yourapp/__init__.py`:

```python
default_app_config = 'yourapp.apps.YourAppConfig'
```

### 4. Міграції

```bash
# Створення міграцій
python manage.py makemigrations

# Застосування міграцій
python manage.py migrate
```

### 5. Ініціалізація даних

```bash
# Створити тільки глобальні статуси
python manage.py init_task_manager --statuses

# Створити демо-дані (проєкти, користувачі, завдання)
python manage.py init_task_manager --demo
```

### 6. Створення суперюзера

```bash
python manage.py createsuperuser
```

### 7. Запуск

```bash
python manage.py runserver
```

Перейдіть в адмінку: http://127.0.0.1:8000/admin/

## 📊 Структура моделей

### Person (Виконавець)
- `full_name` - Прізвище, ім'я
- `surname` - Прізвище окремо
- `position` - Посада
- `group` - Підрозділ (choices: Управління, ІАРМ, ЗБСІ, СД КТК, Майстерня, ПДТР, Служба ТЗІ)
- `is_active` - Активний чи ні

### Project (Проєкт)
- `name` - Назва проєкту
- `key` - Унікальний ключ (наприклад, WEB, INFRA)
- `description` - Опис
- `group` - Основний підрозділ
- `use_custom_statuses` - Використовувати власні статуси
- `is_active` - Активний чи ні

### Status (Статус)
- `name` - Назва статусу
- `color` - Колір (Tailwind CSS класи)
- `order` - Порядок відображення
- `is_default` - Стандартний для нових завдань
- `is_final` - Фінальний статус (автоматично позначає завдання як виконане)
- `project` - Проєкт (null = глобальний статус)

### Task (Завдання)
- `project` - Проєкт
- `key` - Автоматичний ключ (PROJECT-123)
- `title` - Заголовок
- `description` - Опис
- `assignee` - Виконавець
- `department` - Підрозділ
- `created_by` - Автор
- `status` - Поточний статус
- `priority` - Пріоритет (low, medium, high, critical)
- `due_date` - Термін виконання
- `is_completed` - Виконано
- `completed_at` - Дата виконання

### TaskComment (Коментар)
- `task` - Завдання
- `author` - Автор
- `text` - Текст
- `is_system` - Системний коментар

### TaskHistory (Історія)
- `task` - Завдання
- `changed_by` - Хто змінив
- `field_name` - Назва поля
- `old_value` - Старе значення
- `new_value` - Нове значення

## 🎯 Використання

### Створення проєкту

```python
from yourapp.models import Project, PersonGroup

project = Project.objects.create(
    name="Розробка веб-порталу",
    key="WEB",
    description="Корпоративний портал",
    group=PersonGroup.IARM,
    use_custom_statuses=False  # Використовувати глобальні статуси
)
```

### Створення завдання

```python
from yourapp.models import Task, Person, Status

# Отримуємо виконавця та статус
assignee = Person.objects.get(full_name="Петренко Петро")
status = Status.objects.filter(project=None, is_default=True).first()

# Створюємо завдання
task = Task.objects.create(
    project=project,
    title="Розробка API",
    description="Реалізувати REST API",
    assignee=assignee,
    status=status,
    priority="high",
    due_date="2025-12-31"
)

# Ключ генерується автоматично: WEB-1
print(task.key)
```

### Зміна статусу з логуванням

```python
from yourapp.signals import set_task_changed_by

# Встановлюємо хто змінює
set_task_changed_by(task, assignee)

# Змінюємо статус
new_status = Status.objects.get(name="В роботі", project=None)
task.status = new_status
task.save()

# Автоматично створюється запис в історії та системний коментар
```

### Отримання статистики

```python
from yourapp.signals import get_project_statistics, get_user_workload

# Статистика проєкту
stats = get_project_statistics(project)
print(f"Виконано: {stats['completion_rate']}%")
print(f"Середній час: {stats['avg_completion_days']} днів")

# Навантаження користувача
workload = get_user_workload(assignee)
print(f"Активних завдань: {workload['active']}")
print(f"Прострочених: {workload['overdue']}")
```

### Пошук прострочених завдань

```python
from yourapp.signals import get_overdue_tasks, get_tasks_due_soon

# Прострочені завдання
overdue = get_overdue_tasks()

# Завдання, термін яких закінчується найближчим часом (7 днів)
due_soon = get_tasks_due_soon(days=7)
```

## 🎨 Кастомізація

### Додавання нових підрозділів

У `models.py` відредагуйте `PersonGroup`:

```python
class PersonGroup(models.TextChoices):
    GOV = 'management', 'Управління'
    # ... інші підрозділи
    NEW_DEPT = 'new_dept', 'Новий підрозділ'
```

### Додавання нових кольорів статусів

У `admin.py` відредагуйте `color_map`:

```python
color_map = {
    'bg-gray-500': '#6b7280',
    'bg-blue-500': '#3b82f6',
    # ... додайте свої кольори
    'bg-custom-500': '#123456',
}
```

### Додавання валідації

У `models.py` в методі `clean()` моделі:

```python
def clean(self):
    # Ваша валідація
    if self.due_date and self.due_date < timezone.now().date():
        raise ValidationError("Термін не може бути в минулому")
    super().clean()
```

## 📈 API для інтеграції

### Допоміжні функції

```python
from yourapp.signals import (
    set_task_changed_by,           # Встановити автора змін
    bulk_update_task_status,       # Масове оновлення статусу
    get_overdue_tasks,             # Прострочені завдання
    get_tasks_due_soon,            # Завдання що скоро прострочаться
    get_user_workload,             # Навантаження користувача
    get_project_statistics,        # Статистика проєкту
)
```

### Методи моделей

```python
# Project
project.get_tasks_count()           # Загальна кількість завдань
project.get_active_tasks_count()    # Активні завдання
project.get_completed_tasks_count() # Виконані завдання

# Task
task.is_overdue()      # Чи прострочене
task.is_due_today()    # Чи термін сьогодні
task.get_priority_color()  # Колір пріоритету

# Person
person.get_active_tasks_count()  # Активні завдання виконавця
```

## 🔧 Troubleshooting

### Проблема: Сигнали не спрацьовують

Переконайтеся що `apps.py` налаштовано правильно і сигнали імпортуються в `ready()`.

### Проблема: Ключі завдань не генеруються

Перевірте що `key` поле не встановлюється вручну і проєкт має валідний `key`.

### Проблема: Статуси не відображаються

Запустіть `python manage.py init_task_manager --statuses` для створення глобальних статусів.

## 📝 Ліцензія

Цей код надається "як є" для використання в ваших Django проєктах.

## 🤝 Підтримка

Для питань та пропозицій створіть issue в репозиторії проєкту.